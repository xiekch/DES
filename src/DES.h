# pragma once

void encryptData(const char* keyFileName,const char*plainFileName,const char*cipherFileName);
void decryptData(const char* keyFileName,const char*cipherFileName,const char*plainFileName);


